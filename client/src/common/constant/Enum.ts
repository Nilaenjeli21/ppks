export enum Role {
  ADMIN = "ADMIN",
  ADVISER = "ADVISER",
  USER = "USER",
}

export enum ComplaintStatus {
  OPEN = "OPEN",
  IN_PROGRESS = "IN_PROGRESS",
  CLOSED = "CLOSED",
  REJECTED = "REJECTED",
}
